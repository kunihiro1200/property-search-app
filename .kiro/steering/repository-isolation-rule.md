---
inclusion: auto
---

# リポジトリ隔離ルール（絶対に守るべきルール）

## ⚠️ 最重要：間違ったリポジトリにプッシュしない

**2026年2月17日の重大インシデント**:
- `admin-management-backend`ディレクトリが**間違ったリポジトリ**（`property-search-app`）にプッシュされた
- これは物件公開サイトのリポジトリで、本番稼働中
- 業務管理システムのコードをプッシュしてしまい、**物件公開サイトが404エラーで停止**
- 緊急対応で`git reset --hard`と`git push --force`で復旧

**絶対に守るべき原則**:
- **物件公開サイトのリポジトリ（`property-search-app`）には絶対に業務管理システムのコードをプッシュしない**
- **新しいシステムは必ず新しいGitHubリポジトリを作成する**
- **コミット前に必ず`git remote -v`でリポジトリを確認する**

---

## 🚨 このリポジトリ（property-search-app）で許可される変更

### ✅ 許可される変更
1. **物件公開サイト（フロントエンド）**: `frontend/`ディレクトリ
2. **物件公開サイト（バックエンド）**: `backend/api/`ディレクトリ（物件公開サイト専用API）
3. **共通バックエンド**: `backend/src/`ディレクトリ（ローカル環境専用）
4. **ドキュメント**: `.kiro/`、`docs/`、`README.md`など

### ❌ 絶対に禁止される変更
1. **業務管理システムのバックエンドを`admin-management-backend/`ディレクトリに追加**
2. **業務管理システムのコードを`backend/`にマージ**
3. **物件公開サイトのVercel設定（`backend/vercel.json`）を変更**

---

## 📋 正しいリポジトリ構成

### 1. property-search-app（このリポジトリ）
**用途**: 物件公開サイト（本番稼働中）

**ディレクトリ構成**:
```
property-search-app/
├── frontend/              # 物件公開サイト（フロントエンド）
├── backend/
│   ├── api/              # 物件公開サイト専用API（Vercelにデプロイ）
│   ├── src/              # 共通バックエンド（ローカル環境専用）
│   └── vercel.json       # 物件公開サイトのVercel設定
├── .kiro/
└── README.md
```

**Vercelプロジェクト**:
- `property-site-frontend-kappa` - フロントエンド
- `baikyaku-property-site3` - バックエンド（`backend/api/`）

### 2. admin-management-backend（新しいリポジトリ）
**用途**: 業務管理システム（バックエンド）

**ディレクトリ構成**:
```
admin-management-backend/
├── src/                  # 業務管理システムのバックエンド
├── api/                  # Vercelエントリーポイント
├── vercel.json           # 業務管理システムのVercel設定
├── package.json
└── tsconfig.json
```

**Vercelプロジェクト**:
- `admin-management-backend` - バックエンド

### 3. new-admin-management-system（既存リポジトリ）
**用途**: 業務管理システム（フロントエンド）

**Vercelプロジェクト**:
- `new-admin-management-system` - フロントエンド

---

## 🚨 コミット前の必須チェックリスト

### ステップ1: 現在のディレクトリを確認
```bash
pwd
# 出力: /path/to/property-search-app
```

### ステップ2: リポジトリを確認
```bash
git remote -v
# 出力: origin  https://github.com/kunihiro1200/property-search-app.git
```

### ステップ3: 変更内容を確認
```bash
git status
# 確認: admin-management-backend/ が含まれていないか？
```

### ステップ4: 変更内容が正しいか確認
- [ ] 物件公開サイトのファイルのみを変更しているか？
- [ ] `admin-management-backend/`ディレクトリが含まれていないか？
- [ ] `backend/vercel.json`を変更していないか？

### ステップ5: コミット
```bash
git add .
git commit -m "コミットメッセージ"
git push origin main
```

---

## 🚨 絶対にやってはいけないこと

### ❌ 禁止事項1: admin-management-backendをこのリポジトリに追加

```bash
# ❌ 間違い
mkdir admin-management-backend
cd admin-management-backend
git init  # ← これは間違い！このリポジトリのサブディレクトリになる
```

**正しい方法**:
```bash
# ✅ 正しい: 完全に別のディレクトリで新しいリポジトリを作成
cd ..  # property-search-appの外に出る
mkdir admin-management-backend
cd admin-management-backend
git init
git remote add origin https://github.com/kunihiro1200/admin-management-backend.git
```

### ❌ 禁止事項2: 間違ったリポジトリでコミット

```bash
# ❌ 間違い: property-search-appでadmin-management-backendをコミット
cd property-search-app/admin-management-backend
git add .
git commit -m "Add admin backend"  # ← これは間違い！
```

**影響**:
- 物件公開サイトのリポジトリに業務管理システムのコードが混入
- Vercelが誤ったコードをデプロイ
- 物件公開サイトが404エラーで停止

### ❌ 禁止事項3: backend/vercel.jsonを変更

```bash
# ❌ 間違い: 物件公開サイトのVercel設定を変更
vim backend/vercel.json
```

**影響**:
- 物件公開サイトのデプロイが壊れる
- 既存のURLが動作しなくなる

---

## ✅ 正しい業務管理システムのデプロイ手順

### ステップ1: 新しいGitHubリポジトリを作成
1. GitHubで新しいリポジトリ`admin-management-backend`を作成
2. **property-search-appとは完全に別のリポジトリ**

### ステップ2: ローカルで新しいディレクトリを作成
```bash
# property-search-appの外に出る
cd ..

# 新しいディレクトリを作成
mkdir admin-management-backend
cd admin-management-backend

# 新しいリポジトリを初期化
git init
git remote add origin https://github.com/kunihiro1200/admin-management-backend.git
```

### ステップ3: コードをコピー
```bash
# backend/src/の内容をコピー
cp -r ../property-search-app/backend/src ./src

# 必要なファイルを作成
# - api/index.ts
# - vercel.json
# - package.json
# - tsconfig.json
```

### ステップ4: コミット＆プッシュ
```bash
git add .
git commit -m "Initial commit"
git push origin main
```

### ステップ5: Vercelにデプロイ
1. Vercelダッシュボードで「New Project」をクリック
2. `admin-management-backend`リポジトリをインポート
3. 環境変数を設定
4. デプロイ

---

## 📊 リポジトリ一覧

| リポジトリ | 用途 | Vercelプロジェクト | 本番URL |
|-----------|------|-------------------|---------|
| `property-search-app` | 物件公開サイト | `property-site-frontend-kappa`<br>`baikyaku-property-site3` | `https://property-site-frontend-kappa.vercel.app` |
| `admin-management-backend` | 業務管理システム（バックエンド） | `admin-management-backend` | `https://admin-management-backend.vercel.app` |
| `new-admin-management-system` | 業務管理システム（フロントエンド） | `new-admin-management-system` | `https://new-admin-management-system.vercel.app` |

---

## 🔍 トラブルシューティング

### 問題: 間違ったリポジトリにプッシュしてしまった

**症状**:
- 物件公開サイトが404エラーになる
- Vercelのデプロイログに予期しないファイルが表示される

**解決方法**:
1. **すぐに元に戻す**:
   ```bash
   git log --oneline -5  # 正常なコミットIDを確認
   git reset --hard <正常なコミットID>
   git push origin main --force
   ```

2. **Vercelで再デプロイを確認**:
   - Vercelダッシュボードで最新のデプロイを確認
   - 物件公開サイトが正常に動作するか確認

3. **間違ったディレクトリを削除**:
   ```bash
   rm -rf admin-management-backend
   git add .
   git commit -m "Remove admin-management-backend directory"
   git push origin main
   ```

---

## まとめ

**絶対に守るべきルール**:

1. **物件公開サイトのリポジトリ（`property-search-app`）には業務管理システムのコードをプッシュしない**
2. **新しいシステムは必ず新しいGitHubリポジトリを作成する**
3. **コミット前に必ず`git remote -v`でリポジトリを確認する**
4. **`admin-management-backend/`ディレクトリをこのリポジトリに作成しない**
5. **`backend/vercel.json`を変更しない**

**このルールを徹底することで、物件公開サイトの安定性を保ちながら、業務管理システムを安全にデプロイできます。**

---

**最終更新日**: 2026年2月17日  
**作成理由**: 間違ったリポジトリへのプッシュで物件公開サイトが停止した重大インシデントを防ぐため  
**関連インシデント**: 2026年2月17日 - `admin-management-backend`を`property-search-app`にプッシュして物件公開サイトが404エラー
