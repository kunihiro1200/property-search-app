# 買主テーブルのカラム定義（絶対に間違えないルール）

## ⚠️ 重要：買主の識別子

買主を識別する際は、以下のカラム名を使用してください。

---

## ✅ 正しいカラム名

### 買主の識別子

| 用途 | データベースカラム名 | スプレッドシートカラム名 | 説明 |
|------|-------------------|---------------------|------|
| **買主番号**（主キー） | `buyer_number` | `買主番号` | 買主の一意識別子（例: "6666"） |

**重要**: 
- ✅ **`buyer_number`** ← **これが主キー（買主番号）** ← **これだけを使用する**
- ❌ **`buyer_id`** ← **使用しない**（内部的に存在するが、アプリケーションでは使わない）
- ❌ **`id`** ← **存在しない**（絶対に使用しない）

**🚨 最重要ルール**: 
- 買主を識別する際は、**常に`buyer_number`を使用する**
- `buyer_id`は内部的に存在するが、**アプリケーションコードでは一切使用しない**

---

## 🚨 よくある間違い

### ❌ 間違い1: `id`カラムを使用

```typescript
// ❌ 間違い（idカラムは存在しない）
const { data } = await supabase
  .from('buyers')
  .select('*')
  .eq('id', 'some-value');
```

### ❌ 間違い2: `buyer_id`カラムを使用

```typescript
// ❌ 間違い（buyer_idは使用しない）
const { data } = await supabase
  .from('buyers')
  .select('*')
  .eq('buyer_id', 'some-uuid');
```

### ✅ 正しい方法: 買主番号で検索

```typescript
// ✅ 正しい（buyer_numberで検索）
const { data } = await supabase
  .from('buyers')
  .select('*')
  .eq('buyer_number', '6666');
```

---

## 📋 主要カラムのマッピング

### 基本情報

| データベースカラム名 | スプレッドシートカラム名 | 説明 |
|-------------------|---------------------|------|
| `buyer_number` | `買主番号` | 買主の一意識別子 |
| `name` | `●氏名・会社名` | 買主名 |
| `phone` | `●電話番号\n（ハイフン不要）` | 電話番号 |
| `email` | `●メアド` | メールアドレス |
| `inquiry_source` | `●問合せ元` | 問い合わせ元 |

### 問い合わせ情報

| データベースカラム名 | スプレッドシートカラム名 | 説明 |
|-------------------|---------------------|------|
| `property_number` | `物件番号` | 初回問い合わせ物件番号 |
| `inquiry_hearing` | `●問合時ヒアリング` | 問い合わせ時ヒアリング |
| `reception_date` | `受付日` | 受付日 |

### 内覧情報

| データベースカラム名 | スプレッドシートカラム名 | 説明 |
|-------------------|---------------------|------|
| `latest_viewing_date` | `内覧日（最新）` | 最新の内覧日 |
| `viewing_time` | `・時間` | 内覧時間 |
| `follow_up_assignee` | `後続担当` | 後続担当者 |
| `viewing_result_follow_up` | `内覧結果・後続対応` | 内覧結果と後続対応 |

### その他

| データベースカラム名 | スプレッドシートカラム名 | 説明 |
|-------------------|---------------------|------|
| `latest_status` | `★最新状況` | 最新状況 |
| `pre_viewing_notes` | `内覧前伝達事項` | 内覧前伝達事項 |
| `viewing_notes` | `内覧メモ` | 内覧メモ |

---

## 🎯 実装例

### 例1: 買主番号で検索

```typescript
// ✅ 正しい
const { data: buyer } = await supabase
  .from('buyers')
  .select('*')
  .eq('buyer_number', '6666')
  .single();

console.log('買主番号:', buyer.buyer_number);
console.log('買主名:', buyer.name);
console.log('内覧日:', buyer.latest_viewing_date);
```

### 例2: 買主データの更新

```typescript
// ✅ 正しい
await supabase
  .from('buyers')
  .update({
    latest_viewing_date: '2026-02-05',
    viewing_time: '14:30',
    follow_up_assignee: 'Y'
  })
  .eq('buyer_number', '6666');
```

---

## 📝 BuyerServiceの実装

### getById メソッド

**重要**: `getById`メソッドは`buyer_id`（UUID）で検索します。買主番号で検索する場合は`getByBuyerNumber`を使用してください。

```typescript
// ✅ 正しい（buyer_idで検索 - UUID）
async getById(id: string): Promise<any | null> {
  const { data, error } = await this.supabase
    .from('buyers')
    .select('*')
    .eq('buyer_id', id)  // ← buyer_id（UUID）で検索
    .single();
  
  return data;
}

// ✅ 正しい（buyer_numberで検索 - 買主番号）
async getByBuyerNumber(buyerNumber: string): Promise<any | null> {
  const { data, error } = await this.supabase
    .from('buyers')
    .select('*')
    .eq('buyer_number', buyerNumber)  // ← buyer_number（主キー）で検索
    .single();
  
  return data;
}
```

---

## 🔍 トラブルシューティング

### 問題1: "Buyer not found"エラー

**症状**: 
- APIエンドポイント`/api/buyers/:id`が404エラーを返す
- ブラウザで"Buyer not found"と表示される

**原因**:
1. `buyer_id`カラムを使用している（存在しない）
2. `buyer_number`が正しく渡されていない

**解決方法**:
```typescript
// ❌ 間違い
.eq('buyer_id', buyerNumber)

// ✅ 正しい
.eq('buyer_number', buyerNumber)
```

---

### 問題2: UUIDとbuyer_numberの混同

**症状**:
- UUIDを`buyer_number`として使用している
- `buyer_number`をUUIDとして使用している

**解決方法**:
- `buyer_id`（UUID）: データベース内部ID（UUID形式の文字列）
- `buyer_number`（文字列）: 買主番号（例: "6666"）← **これが主キー**

```typescript
// ✅ 正しい使い分け
const buyer = await buyerService.getByBuyerNumber('6666');  // 買主番号で検索
console.log(buyer.buyer_id);  // UUID（内部ID）
console.log(buyer.buyer_number);  // 買主番号（"6666"）← 主キー
```

---

## 📚 関連ドキュメント

- `.kiro/steering/seller-table-column-definition.md` - 売主テーブルのカラム定義（参考）

---

## まとめ

**絶対に守るべきルール**:

1. **買主番号は`buyer_number`** ← これが主キー
2. **UUIDは`buyer_id`** ← 内部ID（UUID形式の文字列）
3. **`id`カラムは存在しない** ← 絶対に使用しない
4. **`getById`メソッドは`buyer_id`で検索** ← UUID検索用
5. **`getByBuyerNumber`メソッドは`buyer_number`で検索** ← 主キー検索用（推奨）

**このルールを徹底することで、カラム名の間違いを完全に防止できます。**

---

**最終更新日**: 2026年2月5日  
**作成理由**: buyer_idとbuyer_numberの混同を防ぎ、カラム名を明確化するため

