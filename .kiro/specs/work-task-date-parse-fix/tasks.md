# 業務リスト日付パース修正 - タスクリスト

## タスク概要

業務リスト（work_tasks）の日付パース処理を修正して、時刻付き日付形式（`YYYY/MM/DD HH:MM:SS`）に対応します。

---

## タスクリスト

- [x] 1. 日付パース処理の修正
  - [x] 1.1 `WorkTaskColumnMapper.ts`の`parseDate`メソッドを修正
  - [x] 1.2 時刻付き日付形式（`YYYY/MM/DD HH:MM:SS`）に対応
  - [x] 1.3 既存の日付形式のサポートを維持

- [x] 2. テストの実行
  - [x] 2.1 ローカルでテストスクリプトを実行
  - [x] 2.2 AA12495のデータ同期状況を確認

- [x] 3. AA12495の再同期
  - [x] 3.1 手動同期を実行
  - [x] 3.2 データベースのデータを確認
  - [x] 3.3 スプレッドシートとデータベースのデータが一致することを確認

---

## タスク詳細

### 1. 日付パース処理の修正

#### 1.1 `WorkTaskColumnMapper.ts`の`parseDate`メソッドを修正

**ファイル**: `backend/src/services/WorkTaskColumnMapper.ts`

**変更内容**:

```typescript
// 変更前
// YYYY/MM/DD形式
if (/^\d{4}\/\d{1,2}\/\d{1,2}$/.test(str)) {
  const parts = str.split('/');
  const year = parts[0];
  const month = parts[1].padStart(2, '0');
  const day = parts[2].padStart(2, '0');
  return `${year}-${month}-${day}`;
}

// 変更後
// YYYY/MM/DD形式（時刻付きも対応）
if (/^\d{4}\/\d{1,2}\/\d{1,2}/.test(str)) {
  // 時刻部分を削除（スペースより前の部分のみを使用）
  const datePart = str.split(' ')[0];
  const parts = datePart.split('/');
  const year = parts[0];
  const month = parts[1].padStart(2, '0');
  const day = parts[2].padStart(2, '0');
  return `${year}-${month}-${day}`;
}
```

**検証**:
- [ ] 正規表現から`$`を削除
- [ ] `str.split(' ')[0]`で時刻部分を削除
- [ ] 日付部分のみを使用してパース

#### 1.2 時刻付き日付形式（`YYYY/MM/DD HH:MM:SS`）に対応

**テストケース**:
- 入力: `"2026/02/15 18:05:00"`
- 期待値: `"2026-02-15"`

**検証**:
- [ ] `"2026/02/15 18:05:00"`が`"2026-02-15"`としてパースされる

#### 1.3 既存の日付形式のサポートを維持

**テストケース**:
1. `YYYY/MM/DD`形式
   - 入力: `"2026/02/15"`
   - 期待値: `"2026-02-15"`

2. `YYYY-MM-DD`形式
   - 入力: `"2026-02-15"`
   - 期待値: `"2026-02-15"`

3. `MM/DD`形式
   - 入力: `"02/15"`
   - 期待値: `"2026-02-15"`（現在の年を使用）

**検証**:
- [ ] 既存の日付形式が正しくパースされる

---

### 2. テストの実行

#### 2.1 ローカルでテストスクリプトを実行

**コマンド**:
```bash
npx ts-node backend/check-aa12495-sync.ts
```

**期待される結果**:
- 修正前: `site_registration_deadline`と`site_registration_due_date`が空文字列
- 修正後: 両方とも正しい日付が表示される（同期後）

**検証**:
- [ ] テストスクリプトが正常に実行される
- [ ] エラーが発生しない

#### 2.2 AA12495のデータ同期状況を確認

**コマンド**:
```bash
npx ts-node backend/check-aa12495-sync.ts
```

**期待される結果**:
```
✅ サイト登録締め日: "2026-02-15"
✅ サイト登録納期予定日: "2026-02-15"
```

**検証**:
- [ ] スプレッドシートとデータベースのデータが一致する

---

### 3. AA12495の再同期

#### 3.1 手動同期を実行

**コマンド**:
```bash
npx ts-node backend/sync-work-tasks.ts
```

**期待される結果**:
- 同期が成功する
- エラーが発生しない

**検証**:
- [ ] 同期が正常に完了する
- [ ] エラーログが出力されない

#### 3.2 データベースのデータを確認

**コマンド**:
```bash
npx ts-node backend/check-aa12495-sync.ts
```

**期待される結果**:
```
✅ サイト登録締め日: "2026-02-15"
✅ サイト登録納期予定日: "2026-02-15"
✅ 決済日: "2026-01-01"
```

**検証**:
- [ ] `site_registration_deadline`が`"2026-02-15"`として保存される
- [ ] `site_registration_due_date`が`"2026-02-15"`として保存される
- [ ] 他のフィールドも正しく同期される

#### 3.3 スプレッドシートとデータベースのデータが一致することを確認

**コマンド**:
```bash
npx ts-node backend/check-aa12495-sync.ts
```

**期待される結果**:
```
✅ 全てのフィールドが一致しています
```

**検証**:
- [ ] 全てのフィールドが一致する
- [ ] 差異がない

---

## 完了条件

- [ ] 全てのタスクが完了している
- [ ] AA12495のデータがスプレッドシートとデータベースで一致している
- [ ] 既存の日付形式が正しくパースされる
- [ ] 他のシステムに影響を与えていない

---

**作成日**: 2026年2月15日  
**作成者**: Kiro AI  
**ステータス**: Not Started
