{
  "name": "property-search-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:frontend": "cd frontend && npm install",
    "build": "cd frontend && NODE_OPTIONS='--max-old-space-size=4096' npm run build",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
